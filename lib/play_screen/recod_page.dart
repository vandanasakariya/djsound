import 'package:flutter/material.dart';

class RecodPage extends StatelessWidget {
  const RecodPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      width: 100,
    );
  }
}
